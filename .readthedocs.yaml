# Read the Docs configuration — builds the Sphinx API docs from the docstrings.
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

sphinx:
  configuration: docs/api/conf.py

python:
  install:
    - requirements: docs/api/requirements.txt
    - requirements: requirements.txt
